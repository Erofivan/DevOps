version: '3.8'
services:
  worker:
    build:
      context: .
      dockerfile: Dockerfile.bad
    
  database:
    image: postgres:latest
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=SuperSecretPassword123
    ports:
      - "5432:5432"